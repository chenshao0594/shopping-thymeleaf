apply plugin: 'java'
apply plugin: 'maven'

dependencies {
    compile project(':common')
	compile group: 'com.paypal.sdk', name: 'rest-api-sdk', version: '1.13.1'
	compile group: 'com.stripe', name: 'stripe-java', version: '5.9.0'

	

    
}